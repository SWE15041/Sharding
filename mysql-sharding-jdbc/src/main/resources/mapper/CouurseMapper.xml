<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.CourseMapper">
    <insert id="insert" parameterType="org.example.domain.Course">
        INSERT INTO
        t_course
        (cid,user_id,corder_no,cname,brief,price,status)
        VALUES
        (#{cid},#{userId},#{corderNo},#{cname},#{brief},#{price},#{status})
    </insert>
    <!--    <select id="queryOrderInfoList" parameterType="com.snowalker.shardingjdbc.snowalker.demo.entity.OrderInfo"-->
    <!--            resultType="com.snowalker.shardingjdbc.snowalker.demo.entity.OrderInfo">-->
    <!--        select-->
    <!--        t.id as id,-->
    <!--        t.user_id as userId,-->
    <!--        t.order_id as orderId,-->
    <!--        t.user_name as userName-->
    <!--        from t_order t-->
    <!--        where t.user_id=#{userId}-->
    <!--    </select>-->

    <!--    <select id="queryOrderInfoByOrderId" parameterType="com.snowalker.shardingjdbc.snowalker.demo.entity.OrderInfo"-->
    <!--            resultType="com.snowalker.shardingjdbc.snowalker.demo.entity.OrderInfo">-->
    <!--        select-->
    <!--        t.id as id,-->
    <!--        t.user_id as userId,-->
    <!--        t.order_id as orderId,-->
    <!--        t.user_name as userName-->
    <!--        from t_order t-->
    <!--        where t.order_id=#{orderId}-->
    <!--        and t.user_id=#{userId}-->
    <!--    </select>-->

    <!--    &lt;!&ndash;订单入库&ndash;&gt;-->
    <!--    <insert id="addOrder"-->
    <!--            parameterType="com.snowalker.shardingjdbc.snowalker.demo.entity.OrderInfo">-->
    <!--        insert into t_order(-->
    <!--        user_id,-->
    <!--        order_id,-->
    <!--        user_name-->
    <!--        )-->
    <!--        values-->
    <!--        (-->
    <!--        #{userId},-->
    <!--        #{orderId},-->
    <!--        #{userName}-->
    <!--        )-->
    <!--    </insert>-->
</mapper>